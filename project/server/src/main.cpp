#include <iostream>
#include <thread>
#include <sstream>

#include "MapRender.h"

int main() {
//    Socket s;
//    s.bind(5000);
//    s.listen();
//
//    while (true) {
//        ClientSocket client = s.accept();
//        std::thread t(client_handler, std::move(client));
//        t.detach();
//    }
}
